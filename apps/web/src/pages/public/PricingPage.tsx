import { PageHeader } from '@/components/public/PageHeader';
import { SectionBand } from '@/components/public/SectionBand';

export function PricingPage(): JSX.Element {
  return (
    <SectionBand>
      <PageHeader title="Pricing" subtitle="Coming soon." />
    </SectionBand>
  );
}
